# Historical Translation Project™ website (Astro + Tailwind)

This is a static Astro site styled to match the navy + gold premium mockup.

## Local dev

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Deploy (Vercel)

1. Push this folder to your GitHub repo (e.g., `historical-translation-project-site`).
2. In Vercel: **New Project → Import** the repo.
3. Framework preset: **Astro**.
4. Build command: `npm run build`
5. Output directory: `dist`
6. Deploy.

## Notes

- Shopify checkout is not integrated yet. Buttons are marked “Coming soon” and email-based notifications are included.
- PDF dossiers in `/public/dossiers/` are placeholders and should be replaced with final PDFs.
- Brand name intentionally uses **Historical Translation Project™** (no “Medical”).

