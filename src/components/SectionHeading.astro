---
const { title, lede } = Astro.props;
---
<div class="mb-8">
  <h2 class="font-serif text-2xl sm:text-3xl tracking-tight text-white">{title}</h2>
  {lede && <p class="mt-3 max-w-3xl text-white/75 leading-relaxed">{lede}</p>}
  <div class="mt-6 gold-hairline"></div>
</div>
